# TinyMCE Editor для уроков

## Описание
В админ-панели используется TinyMCE - полнофункциональный open-source WYSIWYG редактор для редактирования HTML контента уроков с интеграцией загрузки изображений в MinIO/S3.

## Функционал

### Форматирование текста:
- **Жирный** (Ctrl+B)
- *Курсив* (Ctrl+I)
- <u>Подчеркнутый</u> (Ctrl+U)
- ~~Зачеркнутый~~
- Цвет текста и фона

### Заголовки:
- H1, H2, H3, H4, H5, H6
- Параграфы

### Списки:
- Маркированные списки
- Нумерованные списки
- Вложенные списки

### Медиа и контент:
- 🖼 **Загрузка изображений** - прямая загрузка с компьютера в S3
- 🔗 **Ссылки** - вставка гиперссылок
- 📺 **Медиа** - встраивание видео (YouTube, Vimeo и др.)
- ⊞ **Таблицы** - создание и редактирование таблиц
- `</> Код` - блоки кода с подсветкой синтаксиса (HTML, JS, Python, Go и др.)

### Другое:
- Выравнивание текста (слева, по центру, справа, по ширине)
- Отмена/Повтор действий
- Очистка форматирования
- Полноэкранный режим
- Предпросмотр
- Просмотр исходного HTML кода
- Поиск и замена текста

## Технические детали

### Интеграция с MinIO/S3

TinyMCE настроен для автоматической загрузки изображений через backend API:

**Endpoint:** `POST /admin/api/v1/upload/image`

При вставке изображения (drag & drop, clipboard или кнопка "Загрузить"):
1. TinyMCE отправляет файл на `/admin/api/v1/upload/image`
2. Backend сохраняет файл в MinIO/S3 (путь: `images/YYYY/MM/DD/uuid.jpg`)
3. Возвращается публичный URL изображения
4. TinyMCE автоматически вставляет тег `<img>` с URL

**Ограничения:**
- Максимальный размер файла: **10 МБ**
- Поддерживаемые форматы: **JPEG, PNG, GIF, WEBP**

### Сохранение контента

- HTML контент из редактора сохраняется в поле `content` формы
- В базе данных хранится в поле `html_content` таблицы `lesson_d`
- Контент сохраняется "как есть" без дополнительной обработки

### Подключение

**CDN:** TinyMCE загружается с CDN (v6):
```html
<script src="https://cdn.jsdelivr.net/npm/tinymce@6/tinymce.min.js"></script>
```

**Инициализация:** Вызывается функция `initTinyMCE('#lesson-content-editor')` в `wysiwyg-editor.js`

## Использование

1. Перейдите в админ-панель
2. Откройте редактирование урока или создайте новый
3. Используйте панель инструментов TinyMCE для форматирования
4. Для загрузки изображений:
   - Нажмите кнопку "🖼 Image"
   - Выберите файл с компьютера или перетащите изображение в редактор
   - Дождитесь загрузки (появится прогресс-бар)
   - Изображение автоматически вставится с URL из S3
5. HTML контент сохраняется автоматически при отправке формы

## Файлы изменений

### Backend (Go):
- `models/lesson.go` - поле `HTMLContent`
- `handlers/dto/request/lesson.go` - поле в DTO
- `handlers/web/lesson_web_handler.go` - обработка html_content
- `handlers/upload.go` - API для загрузки изображений
- `services/s3.go` - интеграция с MinIO/S3
- `repositories/lesson.go` - SQL запросы

### Frontend:
- `static/js/wysiwyg-editor.js` - инициализация и настройка TinyMCE
- `templates/pages/lesson-form.hbs` - форма с редактором
- `templates/layouts/main.hbs` - подключение TinyMCE CDN

### База данных:
- `init-sql/knowledge-base-db/04-add-html-content-to-lessons.sql` - миграция

## Преимущества TinyMCE

✅ **Open-source** - MIT лицензия, бесплатно для коммерческого использования  
✅ **Полнофункциональный** - богатый набор инструментов из коробки  
✅ **Поддержка RTL** - арабский, иврит и др.  
✅ **Accessibility** - соответствие WCAG 2.1  
✅ **Кроссбраузерность** - работает везде  
✅ **Плагины** - легко расширяется  
✅ **Активная разработка** - регулярные обновления

## Миграция с самописного редактора

Все существующие уроки с HTML контентом останутся без изменений. TinyMCE корректно отображает и редактирует любой валидный HTML.

## Дополнительная настройка

Для изменения конфигурации TinyMCE отредактируйте функцию `initTinyMCE()` в файле [static/js/wysiwyg-editor.js](../static/js/wysiwyg-editor.js).

Документация TinyMCE: https://www.tiny.cloud/docs/

## Миграция базы данных

Для работы редактора необходимо применить миграцию:

### Автоматически (при первом запуске):
Если вы запускаете базу данных впервые, миграция применится автоматически из файла:
```
init-sql/knowledge-base-db/04-add-html-content-to-lessons.sql
```

### Вручную (если база уже запущена):

1. Подключитесь к базе данных:
```bash
docker exec -it app-db psql -U postgres -d knowledge_base_db
```

2. Выполните миграцию:
```sql
ALTER TABLE knowledge_base.lesson_d 
ADD COLUMN IF NOT EXISTS html_content TEXT;

COMMENT ON COLUMN knowledge_base.lesson_d.html_content IS 'HTML контент урока из WYSIWYG редактора';
```

Или:
```bash
docker exec -i app-db psql -U postgres -d knowledge_base_db < init-sql/knowledge-base-db/04-add-html-content-to-lessons.sql
```

## Файлы изменений

### Backend (Go):
- `models/lesson.go` - добавлено поле `HTMLContent`
- `handlers/dto/request/lesson.go` - добавлено поле в DTO
- `handlers/web/lesson_web_handler.go` - обработка html_content в формах
- `repositories/lesson.go` - SQL запросы с полем html_content

### Frontend:
- `static/js/wysiwyg-editor.js` - JavaScript код редактора
- `static/css/wysiwyg-editor.css` - Стили редактора
- `templates/pages/lesson-form.hbs` - Форма с редактором
- `templates/layouts/main.hbs` - Подключение CSS и JS

### База данных:
- `init-sql/knowledge-base-db/04-add-html-content-to-lessons.sql` - Миграция

## Использование

1. Перейдите в админ-панель
2. Откройте редактирование урока или создайте новый
3. Используйте панель инструментов для форматирования текста
4. HTML контент сохраняется автоматически при отправке формы

## Технические детали

- Редактор использует встроенный браузерный API `contentEditable`
- HTML сохраняется в поле `html_content` в таблице `lesson_d`
- Поддержка вставки текста без форматирования (только plain text)
- Responsive дизайн для мобильных устройств
