# Frontend Backlog

Проект: personal-account  
Дата: 12 декабря 2025 г.

Контекст:  
- Шаблоны: Handlebars (`.hbs`)  
- Аутентификация: Keycloak (все действия — редиректы)  
- Фронтенд: vanilla JS + CSS, SSR  
- Токены — только в `HttpOnly` cookies  

---

## 🚀 High Priority (Core Layout & Auth Flow)

| ID    | Задача                     | Подзадачи | Приоритет | Оценка (ч) | Зависимости |
|-------|----------------------------|-----------|-----------|------------|-------------|
| FE-00 | Создать базовый layout (`base.hbs`) | - Структура: `<!DOCTYPE html>`, `<html>`, `<head>` (meta, viewport, `main.css`), `<body>` ⏎ - Логотип в шапке → ссылка на `/` ⏎ - Сайдбар слева: Профиль (/profile), Статистика (/dashboard), Сертификаты (/certificates), Выйти (/auth/logout) ⏎ - Контент-область для `{{{body}}}` ⏎ - Подключение `main.js` перед `</body>` ⏎ - Проверка: `profile.hbs`, `dashboard.hbs`, `certificates.hbs` наследуются корректно | 🔴 High | 4–6 | — |
| FE-01 | Страница-заглушка (`welcome_page.hbs`) | - шаблон (не наследуется от `base.hbs`) ⏎ - Логотип + заголовок ⏎ - Список 3–5 преимуществ ЛК ⏎ - Кнопки: «Войти» → `/auth/login`, «Зарегистрироваться» → `/auth/register`  | 🔴 High | 3–4 | BE-01 |
| FE-02 | Logout (фронтенд) | - Кнопка «Выйти» → `/auth/logout` ⏎ - Сервер: инвалидация сессии + редирект на `/welcome` ⏎ - Проверка: после logout → `/dashboard` → 302 → `/welcome` | 🔴 High | 1–2 | FE-00, BE-03 |

---

## 📊 Medium Priority (Основные разделы ЛК)

| ID    | Задача                             | Подзадачи | Приоритет | Оценка (ч) | Зависимости |
|-------|------------------------------------|-----------|-----------|------------|-------------|
| FE-03 | Страница статистики (`dashboard.hbs`) | - Наследование от `base.hbs` ⏎ - График посещений (X — время, Y — уроки) ⏎ - Список курсов: название, прогресс (%), статус ⏎ - Тесты: «Пройдено: X/Y», «Средний балл: Z%» ⏎ - SSR: данные отображаются без JS | 🟠 Medium | 6–8 | FE-00, BE-10 |
| FE-04 | Страница профиля (`profile.hbs`)   | - Наследование от `base.hbs` ⏎ - Отображение: ФИО, email, ДР, телефон(только чтение) ⏎ - Кнопки-ссылки: «Управление аккаунтом» → `/auth/account`, «Сменить пароль» → `/auth/password` ⏎ - Нет кастомных форм редактирования/удаления | 🟠 Medium | 3–4 | FE-00 |
| FE-05 | Страница сертификатов (`certificates.hbs`) | - Наследование от `base.hbs` ⏎ - Для каждого: название, дата ⏎ - Кнопки: «Открыть» (новая вкладка), «Скачать» (`download`) ⏎ - Ссылки → `/api/certificates/{id}/download` | 🟠 Medium | 4–5 | FE-00, BE-12 |

---

## 🛠 Low Priority / UX Polish

| ID    | Задача                         | Подзадачи | Приоритет | Оценка (ч) | Зависимости |
|-------|--------------------------------|-----------|-----------|------------|-------------|
| FE-06 | Страница ошибки (`error.hbs`)  | - Приём `code`, `message` ⏎ - Отображение: заголовок, описание, кнопка «На главную» ⏎ - Единый стиль (логотип, цвета) ⏎ - Бэкенд: `res.render('error', { code, message })` при ошибках | 🟡 Low | 2 | — |
| FE-07 | Глобальный preloader           | - Простой CSS-спиннер ⏎ - Скрытие по `window.onload` ⏎ - Проверка: не мешает SSR, исчезает после загрузки | 🟡 Low | 1–2 | — |

---

## 🔑 Keycloak Integration — Ссылки

| Action                | URL                        |
|-----------------------|----------------------------|
| Login                 | `api/v1/auth/login`        |
| Register              | `api/v1/auth/register`     |
| Logout                | `api/v1/auth/logout`       |
| Account Management    | `api/v1/auth/account`      |
| Password Change       | `api/v1/auth/password`     |

> ⚠️ Фронтенд **не реализует** формы — только ссылки.

---

# Definition of Done — Frontend  
**Проект**: `personal-account`  
**Дата**: 12 декабря 2025 г.  

> ✅ Применяется ко всем задачам `FE-XX`.  
> 🔒 Фокус: SSR, безопасность (`HttpOnly`), чистота клиентской части, соответствие архитектуре.

---

## 🧱 Архитектура & Интеграция
- [ ] Шаблон корректно наследуется от `base.hbs` (если требуется в бэклоге).  
- [ ] Все аутентификационные действия — **только через редиректы** на `/auth/{login,register,logout,...}`.  
- [ ] Нет чтения/записи токенов в JS: cookies — исключительно `HttpOnly`, фронтенд не знает об их существовании.  
- [ ] Основной контент рендерится на стороне сервера (SSR), без JS-зависимости (если не указано иное).  
- [ ] Все API-запросы (например, `/api/certificates/{id}/download`) используют браузерные credentials (cookies отправляются автоматически).

---

## 🎨 UI / UX / Дизайн
- [ ] Структура: шапка (логотип → `/`), сайдбар, контент-область (`{{{body}}}`).  
- [ ] Все ссылки ведут на правильные эндпоинты (см. таблицу *Keycloak Integration* в бэклоге).  
- [ ] Нет форм авторизации/редактирования на фронте — только ссылки на Keycloak.  
- [ ] Страницы соответствуют ТЗ: элементы, порядок, функциональность (например, кнопки «Скачать», «Открыть» для сертификатов).

---

## 🧪 Проверка
- [ ] Ручная проверка сценариев:  
  - ✅ `/auth/logout` → редирект на `/welcome`, последующий вход в `/dashboard` — 302 → `/welcome`.  
  - ✅ `/welcome` → «Войти» → `/auth/login` → успешный вход → `/dashboard`.  
  - ✅ Все ссылки Keycloak открывают Keycloak (не 404/500).  
- [ ] Валидный HTML5 (без предупреждений в [W3 Nu Validator](https://validator.w3.org/nu)).  
- [ ] Консоль браузера — без ошибок при загрузке и взаимодействии.

---

## 📁 Код & Инфраструктура
- [ ] Нет хардкода: все параметры (URL, тексты) — из `{{data}}` от бэкенда или `.env` (если SSR-рендер).  
- [ ] CSS (`main.css`) и JS (`main.js`) — внешние файлы, подключены в `base.hbs`.  
- [ ] Имена файлов: `*.hbs`, расположение — в `views/` (например, `views/pages/dashboard.hbs`).  
- [ ] Чистый, читаемый Handlebars-код: без логики в шаблонах (только `{{#if}}`, `{{#each}}`, `{{> partial}}`).

---

## 📌 Передача в ревью
- [ ] В PR указано:  
  - Какие страницы/шаблоны изменены.  
  - Как проверить (пошагово, например: *«FE-05: залогиниться → /certificates → клик «Скачать» → должен начаться download без JS»*).  
  - Зависимости от бэкенда (например: *ожидает `/api/v1/user/certificates` с полями `id`, `title`, `issued_at`*).