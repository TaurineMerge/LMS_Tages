# Testing Client

Документация для `app.clients.testing_client.TestingClient` — асинхронного
HTTP-клиента, который обращается к сервису тестирования и сохраняет
сырые ответы в `integration` таблицы через `integration_repository`.

::: app.clients.testing_client.TestingClient
    options:
      show_root_heading: true
      show_source: true

