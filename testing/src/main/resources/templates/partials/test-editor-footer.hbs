<div class="footer-buttons">
    <!-- Кнопка "Назад" -->
    <button type="button" class="btn-secondary" onclick="goBack()">
        ← Назад
    </button>
    
    <!-- Универсальные кнопки сохранения -->
    {{#if isDraft}}
        <!-- Режим черновика -->
        <button type="submit" form="testForm" formaction="/testing/web/tests/draft" class="btn-primary">
            💾 Сохранить черновик
        </button>
        
        <button type="submit" form="testForm" formaction="/testing/web/tests/{{entityId}}/publish" class="btn-success">
            {{#if hasPublishedTest}}🔄 Обновить тест{{else}}🚀 Опубликовать тест{{/if}}
        </button>
        
        <!-- Форма для удаления черновика -->
        <form method="POST" action="/testing/web/tests/{{entityId}}/delete" style="display: inline;">
            <button type="submit" class="btn-danger" 
                    onclick="return confirm('Удалить черновик? Это действие нельзя отменить.')">
                🗑️ Удалить черновик
            </button>
        </form>
        
    {{else}}
        <!-- Режим теста -->
        {{#if isNew}}
            <button type="submit" form="testForm" formaction="/testing/web/tests/save" class="btn-success">
                ✅ Создать тест
            </button>
        {{else}}
            <button type="submit" form="testForm" formaction="/testing/web/tests/save" class="btn-success">
                💾 Сохранить изменения
            </button>
            
            <!-- Форма для удаления теста -->
            <form method="POST" action="/testing/web/tests/{{test.id}}/delete" style="display: inline;">
                <button type="submit" class="btn-danger" 
                        onclick="return confirm('Удалить тест? Все связанные вопросы и ответы будут удалены. Это действие нельзя отменить.')">
                    🗑️ Удалить тест
                </button>
            </form>
            
        {{/if}}
    {{/if}}
</div>