<div class="footer-buttons">
    <!-- Кнопка "Назад" -->
    <button type="button" class="btn-secondary" onclick="goBack()">
        ← Назад
    </button>
    
    <!-- Универсальные кнопки сохранения -->
    {{#if isDraft}}
        <!-- Режим черновика -->
        <button type="submit" form="testForm" formaction="/testing/web/tests/draft" class="btn-primary">
            💾 Сохранить черновик
        </button>
        
        <button type="submit" form="testForm" formaction="/testing/web/tests/{{entityId}}/publish" class="btn-success">
            {{#if hasPublishedTest}}🔄 Обновить тест{{else}}🚀 Опубликовать тест{{/if}}
        </button>
        
        {{!-- <button type="button" class="btn-danger" onclick="deleteEntity('{{entityId}}')">
            🗑️ Удалить черновик
        </button>
 --}}
        <form method="POST" action="/testing/web/tests/{{entityId}}/delete" style="display: inline;">
        <button type="submit" class="btn-danger" onclick="return confirm('Удалить черновик?')">
            🗑️ Удалить черновик
        </button>
    </form>
    {{else}}
        <!-- Режим теста -->
        {{#if isNew}}
            <button type="submit" form="testForm" formaction="/testing/web/tests/save" class="btn-success">
                ✅ Создать тест
            </button>
        {{else}}
            <button type="submit" form="testForm" formaction="/testing/web/tests/save" class="btn-success">
                💾 Сохранить изменения
            </button>
            
            {{!-- <button type="button" class="btn-secondary" onclick="createDraft('{{test.id}}')">
                📝 Редактировать как черновик
            </button> --}}

            
            <button type="button" class="btn-danger" onclick="deleteEntity('{{test.id}}')">
                🗑️ Удалить тест
            </button>
            
        {{/if}}
    {{/if}}
</div>