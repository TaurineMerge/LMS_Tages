<!doctype html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{test.title}}</title>

  <style>
    :root{
      --bg:#fff; --text:#0b0b0b; --muted:#5f5f5f; --border:#e6e6e6;
      --orange:#ff6a00; --orange-weak:rgba(255,106,0,.10); --orange-weak-2:rgba(255,106,0,.06);
      --shadow:0 6px 18px rgba(0,0,0,.06); --radius:16px;
      --danger:#b42318; --danger-weak:rgba(180,35,24,.08);
    }
    *{box-sizing:border-box}
    html{
      /* помогает, чтобы якоря не прятались под sticky topbar */
      scroll-padding-top: 84px;
    }
    body{margin:0;background:var(--bg);color:var(--text);font-family:ui-sans-serif,system-ui,-apple-system,Segoe UI,Roboto,Arial}

    /* убрать подчёркивания по умолчанию */
    a{color:inherit;text-decoration:none}
    a:hover{text-decoration:none}

    .topbar{
      position:sticky;top:0;z-index:5;
      background:rgba(255,255,255,.92);
      backdrop-filter:blur(10px);
      border-bottom:1px solid var(--border);
      padding:12px 0;
      margin-bottom:14px;
      /* фиксируем высоту/поведение, чтобы меньше “прыгало” */
      min-height:64px;
    }
    .topbar-inner{display:flex;align-items:center;justify-content:space-between;gap:12px;max-width:980px;margin:0 auto;padding:0 18px;}
    .top-left{display:flex;flex-direction:column;gap:4px}
    .title{margin:0;font-size:18px;letter-spacing:-.01em;font-weight:600}
    .muted{color:var(--muted);font-size:13px;font-weight:500}

    .top-actions{display:flex;gap:10px;align-items:center;flex-wrap:wrap;justify-content:flex-end}
    .card-actions{display:flex;gap:10px;align-items:center;flex-wrap:wrap}

    .btn{
      padding:11px 16px;border-radius:12px;border:1px solid var(--text);
      background:var(--text);color:#fff;cursor:pointer;
      font-weight:600;letter-spacing:-.01em;
      transition:transform .05s ease,opacity .15s ease,background .15s ease,border-color .15s ease
    }
    .btn:hover{opacity:.94}
    .btn:active{transform:translateY(1px)}
    .btn.pressed{
      background:transparent;color:var(--text);
      border-color:rgba(255,106,0,.85);
      box-shadow:0 0 0 3px rgba(255,106,0,.18);
      cursor:default;opacity:1;
      font-weight:600;
    }
    .btn:disabled{cursor:default;opacity:.75}

    .btn-outline{
      padding:11px 16px;border-radius:12px;border:1px solid rgba(255,106,0,.85);
      background:transparent;color:var(--text);
      font-weight:600;
      box-shadow:0 0 0 3px rgba(255,106,0,.10);
      cursor:pointer;display:inline-flex;align-items:center;justify-content:center;user-select:none;
      white-space:nowrap;
    }
    .btn-outline:hover{box-shadow:0 0 0 3px rgba(255,106,0,.16);}

    .btn-danger{
      padding:12px 18px;
      border-radius:12px;
      border:1px solid rgba(255,106,0,.85);
      background:var(--orange);
      color:#fff;
      cursor:pointer;
      font-weight:650;
      font-size:15px;
      letter-spacing:-.01em;
      white-space:nowrap;
    }
    .btn-danger:hover{opacity:.95}
    .btn-danger:active{transform:translateY(1px)}

    .container{max-width:980px;margin:0 auto;padding:0 18px 60px}
    .card{
      background:#fff;border:1px solid var(--border);
      border-radius:var(--radius);
      box-shadow:var(--shadow);
      padding:16px;margin:14px 0;
      transition:border-color .15s ease,box-shadow .15s ease,background .15s ease;
      /* чтобы якорь позиционировался красивее */
      scroll-margin-top: 84px;
    }
    .card.answered{
      border-color:rgba(255,106,0,.55);
      background:linear-gradient(0deg,var(--orange-weak-2),var(--orange-weak-2)),#fff;
      box-shadow:0 8px 22px rgba(255,106,0,.12),var(--shadow);
    }

    /* подсветка цели по якорю: красным только обводка */
    .card:target{
      border-color:rgba(180,35,24,.75);
      outline:3px solid rgba(180,35,24,.18);
      background:#fff;
    }
    /* если уже отвечено — при target пусть будет оранжевым */
    .card.answered:target{
      border-color:rgba(255,106,0,.75);
      outline:3px solid rgba(255,106,0,.18);
      background:linear-gradient(0deg,var(--orange-weak-2),var(--orange-weak-2)),#fff;
    }

    .qrow{display:grid;grid-template-columns:112px 1fr;gap:14px;align-items:start}
    .points{
      width:112px;padding:12px 10px;border-radius:14px;
      border:1px solid rgba(255,106,0,.35);
      background:linear-gradient(0deg,var(--orange-weak),var(--orange-weak));
      text-align:center;
    }
    .points b{display:block;font-size:20px;letter-spacing:-.02em;line-height:1.1;font-weight:650}
    .points span{display:block;margin-top:2px;font-size:12px;color:var(--muted);font-weight:500}

    .qtitle{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;margin:0 0 8px 0}
    .qtitle h3{margin:0;font-size:16px;letter-spacing:-.01em;line-height:1.25;font-weight:650}
    .qtext{margin:0 0 12px 0;line-height:1.6}

    .answers{display:flex;flex-direction:column;gap:8px;margin:0 0 14px 0;padding:0}

    input{accent-color:var(--orange)}
    input[type="radio"],input[type="checkbox"]{transform:translateY(2px);flex:0 0 auto}

    .answer{
      display:flex;gap:10px;align-items:flex-start;
      padding:10px 12px;border-radius:12px;border:1px solid var(--border);background:#fff;
      transition:border-color .15s ease,background .15s ease;
    }
    .answer:hover{border-color:rgba(255,106,0,.35);background:linear-gradient(0deg,var(--orange-weak-2),var(--orange-weak-2)),#fff}
    .answer.selected{border-color:rgba(255,106,0,.65);background:linear-gradient(0deg,var(--orange-weak),var(--orange-weak)),#fff}
    .answer.disabled{opacity:.85}
    .answer-text{line-height:1.5;min-width:0}

    .badge{
      display:inline-flex;align-items:center;gap:8px;padding:9px 12px;border-radius:999px;
      border:1px solid rgba(255,106,0,.45);
      background:var(--orange-weak);
      font-size:13px;color:#2a2a2a;
      font-weight:500;
    }
    .badge .check{
      width:18px;height:18px;border-radius:999px;
      display:inline-grid;place-items:center;
      background:var(--orange);color:#fff;
      font-size:12px;font-weight:700;
    }

    .error{
      margin:0 0 10px 0;padding:10px 12px;border-radius:14px;
      border:1px solid rgba(180,35,24,.25);background:var(--danger-weak);
      color:var(--danger);font-weight:600;font-size:13px;line-height:1.45;
    }

    /* ---------- МОДАЛКА БЕЗ JS: через :target ---------- */
    .modal{display:none;position:fixed;inset:0;z-index:999;align-items:center;justify-content:center;padding:18px;}
    #finishConfirm:target{display:flex;}
    .modal-overlay{position:absolute;inset:0;background:rgba(0,0,0,.45);}

    .modal-card{
      position:relative;
      width:min(720px,100%);
      background:#fff;
      border-radius:18px;
      border:1px solid var(--border);
      box-shadow:0 18px 60px rgba(0,0,0,.20);
      padding:18px;
    }
    .modal-title{margin:0 0 8px 0;font-size:18px;letter-spacing:-.01em;font-weight:650;}
    .modal-text{margin:0;color:var(--muted);line-height:1.55;font-weight:500;}

    .modal-warn{
      margin-top:12px;
      padding:12px;
      border-radius:14px;
      border:1px solid rgba(255,106,0,.45);
      background:var(--orange-weak);
      color:#2a2a2a;
      font-weight:500;
    }

    .modal-list{margin:10px 0 0 0;padding-left:18px;}
    .modal-list li{margin:10px 0; line-height:1.7; font-weight:500;}

    /* ссылки в перечне — пунктирное оранжевое подчёркивание */
    .miss-link{
      font-weight:500;
      border-bottom: 2px dotted rgba(255,106,0,.75);
      padding-bottom:2px;
    }
    .miss-link:hover{border-bottom-color: rgba(255,106,0,.95);}

    .modal-actions{
      display:flex;gap:10px;flex-wrap:wrap;
      margin-top:14px;align-items:center;justify-content:flex-end;
    }

    .footer{text-align:center;color:var(--muted);padding:22px 0 10px;font-weight:500;}

    @media (max-width:640px){
      .topbar-inner{flex-direction:column;align-items:flex-start}
      .top-actions{width:100%;justify-content:flex-start}
      .qrow{grid-template-columns:1fr}
      .points{width:100%;text-align:left;display:flex;align-items:baseline;justify-content:space-between}
      .points b{font-size:18px}
      .points span{margin-top:0}
      .modal-actions{justify-content:stretch}
      .modal-actions>*{flex:1}
    }
  </style>
</head>

<body>

  <div class="topbar">
    <div class="topbar-inner">
      <div class="top-left">
        <div class="title">{{test.title}}</div>
        <div class="muted">
          Попытка: <b style="font-weight:650">{{attempt_no}}</b> · Прогресс: <b style="font-weight:650">{{answeredCount}}</b> / <b style="font-weight:650">{{totalQuestions}}</b>
          {{#if test.min_point}} · Проходной: <b style="font-weight:650">{{test.min_point}}</b>{{/if}}
        </div>
      </div>

      <div class="top-actions">
        {{#if allAnswered}}
          <form method="post" action="/testing/ui/tests/{{test.id}}/finish" style="margin:0;">
            <input type="hidden" name="attempt_id" value="{{attempt_id}}"/>
            <input type="hidden" name="student_id" value="{{student_id}}"/>
            <input type="hidden" name="force" value="true"/>
            <button class="btn-outline" type="submit">Завершить и посмотреть результаты</button>
          </form>
        {{else}}
          <a class="btn-outline" href="#finishConfirm">Завершить и посмотреть результаты</a>
        {{/if}}
      </div>
    </div>
  </div>

  <div class="container">
    {{#each questions}}
      <div class="card {{#if answered}}answered{{/if}}" id="q_{{id}}">
        <div class="qrow">
          <div class="points">
            <b>{{maxPoints}}</b>
            <span>макс. балл</span>
          </div>

          <div>
            <div class="qtitle">
              <h3>Вопрос {{order}}</h3>
            </div>

            <p class="qtext">{{text_of_question}}</p>
            {{#if hint}}<div class="muted" style="margin:-6px 0 10px 0;"><span style="font-weight:600">{{hint}}</span></div>{{/if}}

            {{#if validationError}}
              <div class="error">{{validationError}}</div>
            {{/if}}

            <form method="post" action="/testing/ui/tests/{{../test.id}}/questions/{{id}}/answer">
              <div class="answers">
                {{#each answers}}
                  <label class="answer {{#if disabled}}disabled{{/if}} {{#if selected}}selected{{/if}}">
                    <input type="{{../inputType}}" name="{{../inputName}}" value="{{id}}"
                      {{#if selected}}checked{{/if}}
                      {{#if disabled}}disabled{{/if}} />
                    <span class="answer-text">{{text}}</span>
                  </label>
                {{/each}}
              </div>

              <input type="hidden" name="student_id" value="{{../student_id}}" />
              <input type="hidden" name="attempt_id" value="{{../attempt_id}}" />

              <div class="card-actions">
                <button type="submit" class="{{buttonClass}}" {{#if buttonDisabled}}disabled{{/if}}>
                  {{buttonText}}
                </button>

                {{#if answered}}
                  <div class="badge">
                    <span class="check">✓</span>
                    <span>Сохранено{{#if selectedAnswerText}}: <span style="font-weight:600">{{selectedAnswerText}}</span>{{/if}}</span>
                  </div>
                {{/if}}
              </div>
            </form>
          </div>
        </div>
      </div>
    {{/each}}
  </div>

  {{#unless allAnswered}}
    <div class="modal" id="finishConfirm" aria-modal="true" role="dialog">
      <a class="modal-overlay" href="#" aria-label="Закрыть"></a>

      <div class="modal-card">
        <h2 class="modal-title">Завершить попытку?</h2>
        <p class="modal-text">
          Есть вопросы без ответа: {{missingCount}}. Можно вернуться и ответить, либо завершить и посмотреть результаты.
        </p>

        <div class="modal-warn">
          <div style="margin-bottom:6px;">Без ответа:</div>
          <ul class="modal-list">
            {{#each missingQuestions}}
              <li><a class="miss-link" href="#q_{{id}}">Вопрос {{order}}</a></li>
            {{/each}}
          </ul>
        </div>

        <div class="modal-actions">
          <a class="btn-outline" href="#">Продолжить тестирование</a>

          <form method="post" action="/testing/ui/tests/{{test.id}}/finish" style="margin:0;">
            <input type="hidden" name="attempt_id" value="{{attempt_id}}"/>
            <input type="hidden" name="student_id" value="{{student_id}}"/>
            <input type="hidden" name="force" value="true"/>
            <button class="btn-danger" type="submit">Завершить и смотреть результаты</button>
          </form>
        </div>
      </div>
    </div>
  {{/unless}}

  <footer class="footer">
    {{> footer}}
  </footer>
</body>
</html>