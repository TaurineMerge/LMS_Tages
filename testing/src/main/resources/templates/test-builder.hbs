<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
:root{
  --bg:#fff; --text:#0b0b0b; --muted:#5f5f5f; --border:#e6e6e6;
  --orange:#ff6a00; --orange-weak:rgba(255,106,0,.10); --orange-weak-2:rgba(255,106,0,.06);
  --shadow:0 6px 18px rgba(0,0,0,.06); --radius:16px;
  --danger:#b42318; --danger-weak:rgba(180,35,24,.08);
}
*{box-sizing:border-box}
html{scroll-padding-top:84px;}
body{
  margin:0;
  background:var(--bg);
  color:var(--text);
  font-family:ui-sans-serif,system-ui,-apple-system,Segoe UI,Roboto,Arial;
  padding:0 0 120px;
}
a{color:inherit;text-decoration:none}
a:hover{text-decoration:none}

.container{max-width:980px;margin:0 auto;padding:18px 18px 60px}

h1{margin:0 0 14px 0;font-size:22px;letter-spacing:-.01em;font-weight:650}

.card{
  background:#fff;border:1px solid var(--border);
  border-radius:var(--radius);box-shadow:var(--shadow);
  padding:16px;margin:14px 0;
}

.question-card{
  background:#fff;border:1px solid var(--border);
  border-radius:var(--radius);padding:16px;margin:14px 0;
  box-shadow:var(--shadow);
}

.form-group{margin-bottom:14px}
label{display:block;margin-bottom:6px;font-size:13px;color:var(--muted);font-weight:600}
input,textarea,select{
  width:100%;padding:11px 12px;border:1px solid var(--border);
  border-radius:12px;background:#fff;color:var(--text);
  font:inherit;outline:none;
  transition:border-color .15s ease,box-shadow .15s ease;
}
textarea{min-height:100px;resize:vertical;line-height:1.55}
input:focus,textarea:focus,select:focus{
  border-color:rgba(255,106,0,.75);
  box-shadow:0 0 0 3px rgba(255,106,0,.14);
}

button{border:none;background:transparent;font:inherit}

.btn-primary,.btn{
  padding:11px 16px;border-radius:12px;border:1px solid var(--text);
  background:var(--text);color:#fff;cursor:pointer;
  font-weight:600;letter-spacing:-.01em;
  transition:transform .05s ease,opacity .15s ease,background .15s ease,border-color .15s ease;
}
.btn-primary:hover,.btn:hover{opacity:.94}
.btn-primary:active,.btn:active{transform:translateY(1px)}
.btn-primary:disabled,.btn:disabled{cursor:default;opacity:.75}

.btn-secondary,.btn-outline{
  padding:11px 16px;border-radius:12px;border:1px solid rgba(255,106,0,.85);
  background:transparent;color:var(--text);font-weight:600;
  box-shadow:0 0 0 3px rgba(255,106,0,.10);
  cursor:pointer;display:inline-flex;align-items:center;justify-content:center;user-select:none;
  white-space:nowrap;
}
.btn-secondary:hover,.btn-outline:hover{box-shadow:0 0 0 3px rgba(255,106,0,.16);}

.btn-success{
  padding:12px 18px;border-radius:12px;border:1px solid rgba(255,106,0,.85);
  background:var(--orange);color:#fff;cursor:pointer;
  font-weight:650;font-size:15px;letter-spacing:-.01em;white-space:nowrap;
}
.btn-success:hover{opacity:.95}
.btn-success:active{transform:translateY(1px)}

.btn-danger{
  padding:11px 16px;border-radius:12px;border:1px solid rgba(180,35,24,.55);
  background:transparent;color:var(--danger);cursor:pointer;
  font-weight:650;letter-spacing:-.01em;white-space:nowrap;
}
.btn-danger:hover{background:var(--danger-weak)}
.btn-danger:active{transform:translateY(1px)}
.btn-danger:disabled{opacity:.6;cursor:default}

.alert{
  padding:10px 12px;margin:10px 0;border-radius:14px;
  font-weight:600;font-size:13px;line-height:1.45;
}
.alert-success{border:1px solid rgba(255,106,0,.45);background:var(--orange-weak);color:#2a2a2a}
.alert-error{border:1px solid rgba(180,35,24,.25);background:var(--danger-weak);color:var(--danger)}

.answers-container{margin:12px 0}
.answer-group{
  display:flex;align-items:flex-start;gap:10px;
  padding:10px 12px;border-radius:12px;border:1px solid var(--border);background:#fff;
  transition:border-color .15s ease,background .15s ease;
  margin-bottom:10px;
}
.answer-group:hover{
  border-color:rgba(255,106,0,.35);
  background:linear-gradient(0deg,var(--orange-weak-2),var(--orange-weak-2)),#fff;
}
.answer-group input[type="text"]{flex:1}

.answer-points-group{display:flex;align-items:center;gap:8px;flex:0 0 auto}
.answer-points-group label{margin:0;font-size:13px;color:var(--muted);font-weight:600}
.answer-points-group input{width:86px;padding:9px 10px}

.question-controls{display:flex;gap:6px;flex-wrap:wrap;justify-content:flex-end;margin-top:8px}
.btn-move-up,.btn-move-down,.remove-question-btn,.remove-answer-btn,.add-answer-btn,#add-question-btn{
  padding:9px 12px;border-radius:12px;border:1px solid var(--border);
  background:#fff;cursor:pointer;font-weight:600;font-size:13px;
  transition:transform .05s ease,opacity .15s ease,border-color .15s ease,box-shadow .15s ease,background .15s ease;
}
.btn-move-up:hover,.btn-move-down:hover,.add-answer-btn:hover,#add-question-btn:hover{
  border-color:rgba(255,106,0,.55);
  box-shadow:0 0 0 3px rgba(255,106,0,.10);
}
.btn-move-up:active,.btn-move-down:active,.add-answer-btn:active,#add-question-btn:active,
.remove-question-btn:active,.remove-answer-btn:active{transform:translateY(1px)}
.remove-question-btn,.remove-answer-btn{border-color:rgba(180,35,24,.25);color:var(--danger)}
.remove-question-btn:hover,.remove-answer-btn:hover{background:var(--danger-weak);border-color:rgba(180,35,24,.35)}

.question-points-info{
  margin:12px 0;padding:12px;border-radius:14px;
  border:1px solid rgba(255,106,0,.45);background:var(--orange-weak);
  color:#2a2a2a;font-size:13px;
}
.points-display{display:flex;align-items:center;gap:8px;margin-bottom:4px}
.total-answers-points{color:var(--orange);font-size:16px;min-width:40px;text-align:center}
.points-hint{font-size:12px;color:var(--muted);font-style:italic;font-weight:500}

.total-test-points-display{
  margin:12px 0 0 0;padding:12px;border-radius:14px;
  border:1px solid rgba(255,106,0,.45);background:var(--orange-weak);
  color:#2a2a2a;font-weight:650;text-align:center;
}
#total-test-points{color:var(--orange);font-size:20px;font-weight:750}

.footer{
  position:fixed;bottom:0;left:0;right:0;
  background:rgba(255,255,255,.92);backdrop-filter:blur(10px);
  border-top:1px solid var(--border);
  padding:12px 0;box-shadow:0 -8px 24px rgba(0,0,0,.06);
}
.footer .container{padding:0 18px;max-width:980px}
.footer-buttons{display:flex;gap:10px;justify-content:flex-end;flex-wrap:wrap}

@media (max-width:640px){
  .footer-buttons>*{flex:1}
  .answer-group{flex-direction:column;align-items:stretch}
  .question-controls{justify-content:flex-start}
}
</style>
</head>
<body>
    <div class="container">
        <h1>{{title}}</h1>
        
        {{#if success}}
        <div class="alert alert-success">
            ✅ Тест успешно сохранен!
        </div>
        {{/if}}
        
        {{#if error}}
        <div class="alert alert-error">
            ❌ {{error}}
        </div>
        {{/if}}
        
        <!-- Вставляем основную форму -->
        {{> partials/test-builder-form}}
    </div>
    
    <div class="footer">
        <div class="container">
            <!-- Вставляем кнопки футера -->
            {{> partials/test-builder-footer}}
        </div>
    </div>
    
    <!-- Вставляем скрипты -->
    {{> partials/test-builder-scripts}}
</body>
</html>