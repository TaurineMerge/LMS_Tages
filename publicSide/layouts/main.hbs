<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{Title}}</title>
    
    <!-- Bootstrap для быстрого стиля -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <style>
        body {
            padding-top: 70px;
            font-family: Arial, sans-serif;
        }
        .hero-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 100px 0;
            border-radius: 0 0 30px 30px;
        }
        .course-card {
            border: 1px solid #ddd;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
            transition: transform 0.3s;
        }
        .course-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }
        .btn-admin {
            background: #ffc107;
            border: none;
            font-weight: bold;
        }
        .btn-swagger {
            background: #17a2b8;
            border: none;
            color: white;
        }
        .port-badge {
            font-size: 0.7rem;
            opacity: 0.8;
        }
    </style>
</head>
<body>
    <!-- Навигация с прямыми ссылками -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">📚 Tages LMS</a>
            <div class="navbar-nav">
                <a class="nav-link" href="/">Главная</a>
                <a class="nav-link" href="/courses">Курсы</a>
                <!-- Прямые ссылки на другие сервисы -->
                <a class="nav-link" href="http://localhost:4000" target="_blank">
                    Админка <span class="port-badge">:4000</span>
                </a>
                <a class="nav-link" href="http://localhost:4000/admin/swagger/" target="_blank">
                    API Docs <span class="port-badge">Swagger</span>
                </a>
            </div>
        </div>
    </nav>

    <main class="container">
        <!-- Сюда подставляется контент страницы -->
        {{{body}}}
    </main>

    <!-- Футер с прямыми ссылками -->
    <footer class="mt-5 py-4 bg-light">
        <div class="container text-center">
            <p>Tages LMS © 2025</p>
            <p class="text-muted small">
                Публичная часть | 
                <a href="http://localhost:4000" target="_blank">Админ-панель</a> | 
                <a href="http://localhost:4000/admin/swagger/" target="_blank">Swagger API</a>
            </p>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>