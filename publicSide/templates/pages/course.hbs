{{> partials/page_header PageHeader}}

<div class="course-page">
    <div class="course-page__main">
        <div class="course-info">
            <div class="course-info__header">
                <div class="course-info__icon">📖</div>
                <div class="course-info__meta">
                    <div class="course-info__meta-row">
                        <span class="course-info__level course-info__level--{{Course.Level}}">
                            {{LevelRu}}
                        </span>
                        <span class="course-info__date">
                            Обновлено: {{formatDate Course.UpdatedAt}}
                        </span>
                    </div>
                </div>
            </div>

            <div class="course-info__description">
                <h2>О курсе</h2>
                {{#if Course.Description}}
                    <p>{{Course.Description}}</p>
                {{else}}
                    <p>Описание курса отсутствует.</p>
                {{/if}}
            </div>

            <div class="course-info__stats">
                <div class="course-stat">
                    <div class="course-stat__icon">📚</div>
                    <div class="course-stat__content">
                        <div class="course-stat__value">10</div>
                        <div class="course-stat__label">Уроков</div>
                    </div>
                </div>
                <div class="course-stat">
                    <div class="course-stat__icon">⏱️</div>
                    <div class="course-stat__content">
                        <div class="course-stat__value">20 ч</div>
                        <div class="course-stat__label">Длительность</div>
                    </div>
                </div>
                <div class="course-stat">
                    <div class="course-stat__icon">👥</div>
                    <div class="course-stat__content">
                        <div class="course-stat__value">1,234</div>
                        <div class="course-stat__label">Студентов</div>
                    </div>
                </div>
            </div>

            <div class="course-info__actions">
                {{#if FirstLessonID}}
                <a href="/categories/{{CategoryID}}/courses/{{Course.ID}}/lessons/{{FirstLessonID}}" class="button button--primary button--large">
                    Перейти к урокам
                </a>
                {{else}}
                <a href="/categories/{{CategoryID}}/courses/{{Course.ID}}/lessons" class="button button--primary button--large">
                    Перейти к урокам
                </a>
                {{/if}}
                <a href="/categories/{{CategoryID}}/courses" class="button button--secondary button--large">
                    Назад к курсам
                </a>
            </div>
        </div>
    </div>
</div>
