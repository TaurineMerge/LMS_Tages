<!-- templates/pages/categories.hbs -->

<section class="courses">
    <header class="page-header">
        <div class="page-header__breadcrumbs">
            <a href="/">Главная</a>
            <span class="page-header__separator">/</span>
            <span>Категории</span>
        </div>
        <h1 class="page-header__title">Категории курсов</h1>
    </header>

    {{#each categories}}
        <section class="courses-category">
            <div class="courses__header">
                <h2 class="courses__subtitle">{{Title}}</h2>

                {{#if TotalCourses}}
                    <span class="courses__count">{{TotalCourses}} курсов</span>
                {{/if}}
            </div>

            {{#if Courses}}
                <div class="courses__grid">
                    {{#each Courses}}
                        <article class="course-card">
                            <div class="course-card__image">
                                <span>📚</span>
                            </div>
                            <div class="course-card__content">
                                <h3 class="course-card__title">{{Title}}</h3>
                            </div>
                        </article>
                    {{/each}}
                </div>
            {{else}}
                <div class="empty-state">
                    <p class="empty-state__text">
                        В этой категории пока нет доступных курсов.
                    </p>
                </div>
            {{/if}}

            {{#if HasMoreCourses}}
                <div class="courses-category__actions">
                    <a href="/categories/category_{{ID}}/courses" class="button button--primary">
                        Показать ещё
                    </a>
                </div>
            {{/if}}
        </section>
    {{/each}}

    {{^categories}}
        <div class="empty-state">
            <p class="empty-state__text">
                Пока нет ни одной категории. Создайте категории через Swagger admin, и они появятся здесь автоматически.
            </p>
        </div>
    {{/categories}}
</section>

