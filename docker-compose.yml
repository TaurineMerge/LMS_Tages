services:
  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/includes:/etc/nginx/includes:ro
      - ./personal-account/docs-tech:/var/www/docs-tech:ro
      - ./personal-account/static:/etc/nginx/html/static:ro
    depends_on:
      keycloak:
        condition: service_healthy
      public-side:
        condition: service_started
      admin-panel:
        condition: service_started
      personal-account:
        condition: service_started
      testing:
        condition: service_started
      jaeger:
        condition: service_started
      otel-collector:
        condition: service_started
    networks:
      - app-network
    restart: unless-stopped

  keycloak-db:
    image: postgres:15
    container_name: keycloak-db
    environment:
      POSTGRES_DB: keycloaks
      POSTGRES_USER: keycloaks
      POSTGRES_PASSWORD: keycloaks
    volumes:
      - keycloak_data:/var/lib/postgresql/data
    networks:
      - keycloak-network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U keycloaks -d keycloaks" ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  keycloak:
    image: quay.io/keycloak/keycloak:23.0.0
    container_name: keycloak
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://keycloak-db:5432/keycloaks
      KC_DB_USERNAME: keycloaks
      KC_DB_PASSWORD: keycloaks
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_HEALTH_ENABLED: true
      KC_METRICS_ENABLED: true
      KC_HOSTNAME_STRICT: "false"
      KC_HOSTNAME_STRICT_HTTPS: "false"
      KC_PROXY: "reencrypt"
      KC_HOSTNAME: "localhost"
      KC_HOSTNAME_PORT: "80"
      KC_HOSTNAME_STRICT_BACKCHANNEL: "false"
      KC_HTTP_RELATIVE_PATH: "/auth"
      KC_BASE_URL: "http://localhost/auth"
    command:
      - start-dev
      - --http-port=8080
      - --import-realm
      - --hostname-strict-https=false
      - --http-relative-path=/auth
      - --hostname-admin-url=http://localhost/auth
    ports: []
    volumes:
      - ./keycloak/student.json:/opt/keycloak/data/import/student.json:ro
      - ./keycloak/teacher.json:/opt/keycloak/data/import/teacher.json:ro
    depends_on:
      keycloak-db:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/localhost/8080 && echo -e 'GET /auth/health/ready HTTP/1.1\\r\\nHost: localhost\\r\\n\\r\\n' >&3 && grep -q '200 OK' <&3 || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 60s
    networks:
      - keycloak-network
      - app-network
    restart: unless-stopped

  app-db:
    image: postgres:15
    container_name: app-db
    environment:
      POSTGRES_DB: appdb
      POSTGRES_USER: appuser
      POSTGRES_PASSWORD: password
    # ports:
    #   - "5432:5432"
    volumes:
      - app_data:/var/lib/postgresql/data
      - ./init-sql:/docker-entrypoint-initdb.d:ro
    networks:
      - app-network
    ports: []
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U appuser -d appdb" ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  knowledge-base-db:
    image: postgres:15
    container_name: knowledge-base-db
    environment:
      POSTGRES_DB: ${KNOWLEDGE_BASE_DB_NAME}
      POSTGRES_USER: ${KNOWLEDGE_BASE_DB_SUPERUSER}
      POSTGRES_PASSWORD: ${KNOWLEDGE_BASE_DB_SUPERUSER_PASSWORD}
    env_file:
      - ./.env
    volumes:
      - knowledge_base_data:/var/lib/postgresql/data
      - ./init-sql/knowledge-base-db:/docker-entrypoint-initdb.d:ro
    networks:
      - knowledge-base-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${KNOWLEDGE_BASE_DB_SUPERUSER} -d ${KNOWLEDGE_BASE_DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  public-side:
    build: ./publicSide
    container_name: public-side
    ports: []
    environment:
      DB_NAME: ${KNOWLEDGE_BASE_DB_NAME}
      DB_USER: ${KNOWLEDGE_BASE_RO_USER}
      DB_PASSWORD: ${KNOWLEDGE_BASE_RO_PASSWORD}
      DB_HOST: ${KNOWLEDGE_BASE_DB_HOST}
      DB_PORT: ${KNOWLEDGE_BASE_DB_PORT}
    env_file:
      - ./.env
      - ./publicSide/.env.prod
    networks:
      - app-network
      - knowledge-base-network
    depends_on:
      knowledge-base-db:
        condition: service_healthy
      keycloak:
        condition: service_started
    restart: unless-stopped

  admin-panel:
    build: ./adminPanel
    container_name: admin-panel
    env_file:
      - ./adminPanel/.env.prod
    environment:
      # Database
      DATABASE_URL: "postgresql://appuser:password@app-db:5432/appdb?sslmode=disable"
      DATABASE_POOL_MIN_SIZE: "5"
      DATABASE_POOL_MAX_SIZE: "20"
      # Keycloak
      KEYCLOAK_ISSUER_URL: "http://keycloak:8080/auth/realms/teacher"
      KEYCLOAK_CLIENT_ID: "teacher-client"
      KEYCLOAK_CLIENT_SECRET: "TEACHER_SECRET"
      KEYCLOAK_APP_NAME: "LMS Admin Application"
      # OpenTelemetry
      OTEL_EXPORTER_OTLP_ENDPOINT: "http://otel-collector:4317"
      OTEL_EXPORTER_OTLP_PROTOCOL: "grpc"
      OTEL_SERVICE_NAME: "admin-panel"
      # Server
      API_ADDRESS: ":4000"
      # CORS
      CORS_ALLOW_ORIGINS: "*"
      CORS_ALLOW_CREDENTIALS: "false"
    ports: []
    networks:
      - app-network
      - knowledge-base-network
    depends_on:
      knowledge-base-db:
        condition: service_healthy
      keycloak:
        condition: service_healthy
    restart: unless-stopped

  personal-account:
    build: ./personal-account
    container_name: personal-account
    ports: []
    env_file:
      - ./personal-account/.env
    environment:
      OTEL_EXPORTER_OTLP_ENDPOINT: "${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4317}"
      OTEL_SERVICE_NAME: "${OTEL_SERVICE_NAME:-personal-account-api}"
    networks:
      - app-network
    depends_on:
      app-db:
        condition: service_healthy
      keycloak:
        condition: service_started
    restart: unless-stopped

  testing:
    build: ./testing
    container_name: testing
    ports: []
    env_file:
      - ./testing/.env
    networks:
      - app-network
    depends_on:
      app-db:
        condition: service_healthy
      keycloak:
        condition: service_started
    restart: unless-stopped

  jaeger:
    image: jaegertracing/all-in-one:1.54
    container_name: jaeger
    environment:
      - COLLECTOR_ZIPKIN_HTTP_PORT=9411
    ports:
      - "16686:16686" # UI
      - "14250:14250" # gRPC ingestion
      - "14268:14268" # HTTP ingestion
      - "9411:9411" # Zipkin compatibility
    networks:
      - app-network
    restart: unless-stopped

  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.94.0
    container_name: otel-collector
    command: [ "--config=/etc/otel-collector-config.yaml" ]
    volumes:
      - ./observability/otel-collector-config.yaml:/etc/otel-collector-config.yaml:ro
    ports:
      - "4317:4317" # OTLP gRPC
      - "4318:4318" # OTLP HTTP
    depends_on:
      jaeger:
        condition: service_started
    networks:
      - app-network
    restart: unless-stopped

networks:
  keycloak-network:
    driver: bridge
    internal: true
  app-network:
    driver: bridge
  knowledge-base-network:
    driver: bridge

volumes:
  keycloak_data:
    driver: local
  app_data:
    driver: local
  knowledge_base_data:
    driver: local
